{
  "name": "goal-tracker",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --parallel dev:api dev:web",
    "dev:api": "bun run --filter api dev",
    "dev:web": "bun run --filter web dev",
    "setup:db": "cd apps/api && npx wrangler d1 execute goal-tracker-db --local --file=src/db/migrations/0001_initial.sql && npx wrangler d1 execute goal-tracker-db --local --file=src/db/migrations/0002_add_coaching.sql"
  }
}
